<!DOCTYPE html>
<html>
    <head>
        <title>Yelp camp</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
        <link rel="stylesheet" href="/stylesheet/main.css">
    </head>
    <body>
        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand" href="/">YelpCamp</a>
                    <% if(!currentuser){ %>
                        <a class="navbar-brand" href="/login">login</a>
                        <a class="navbar-brand" href="/register">signUp</a>
                    <% }else{ %>
                        <li class="i"><a href="#">signed in as <%= currentuser.username %></a></li>
                        <a class="navbar-brand" href="/logout">logout</a>
                    <% } %>
                </div>
                <!--<div class="collapse navbar-collapse">-->
                <!--    <ul class="nav navbar-nav navbar-right">-->
                <!--        <li class="i"><a href="/login">Login</a></li>-->
                <!--        <li class="i"><a href="/register">SignUp</a></li>-->
                <!--        <li class="i"><a href="/logout">Logout</a></li>-->
                <!--    </ul>-->
                <!--</div>-->
            </div>
        </nav>
    <div class="container">
        <% if(error && error.length>0) {%>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length>0){ %>
            <div class="alert alert-danger" role="alert">
                <%= success %>
            </div>
        <% } %>    
    </div>
    